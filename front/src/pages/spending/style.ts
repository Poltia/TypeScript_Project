import { BaseButton } from "@/assets/baseStyle";
import styled from "styled-components";

export const SpendingContainer = styled.div``;

export const SpendingButton = styled(BaseButton)`
  background-color: beige;
`;
